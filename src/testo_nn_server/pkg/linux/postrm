#!/bin/bash -e

if command -v systemctl >/dev/null 2>&1; then
    systemctl daemon-reload 2>/dev/null || true
fi
if [ "$1" = "purge" ]; then
    rm -f /var/log/testo-nn-server.log /var/log/testo_nn_server.log
fi
