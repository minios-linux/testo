#!/bin/bash

self="$(readlink -f -- "$0")"
prefix="$(dirname -- "$(dirname -- "$self")")"
export LD_LIBRARY_PATH="$prefix/lib/testo${LD_LIBRARY_PATH:+:$LD_LIBRARY_PATH}"

exec "$prefix/libexec/testo" "$@"
